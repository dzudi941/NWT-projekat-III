class User
{
	constructor(fullName, email, password, passwordConfirm, usertype) {
		this.fullName = fullName;
		this.email = email;
		this.password = password;
		this.confirmPassword = passwordConfirm;
		this.userType = usertype;
	}
}